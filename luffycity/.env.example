# Copy to .env.development / .env.production and adjust host.
# Vue CLI only exposes variables prefixed with VUE_APP_* to the client bundle.
#
# 开发默认（见仓库 .env.development）：相对路径 + vue-cli proxy，避免跨域
# VUE_APP_API_BASE_URL=/api/v1/
#
# 或直连 Django（需后端 corsheaders 在中间件较前位置）：
# VUE_APP_API_BASE_URL=http://127.0.0.1:8000/api/v1/
#
# devServer 代理目标（仅 vue.config.js 读取，不注入浏览器包）：
# DJANGO_DEV_TARGET=http://127.0.0.1:8000